import type {Project} from "@/data/portfolio";
export function ProjectCard({project,index}:{project:Project;index:number}){return <article className="project-card"><div className="project-index">0{index+1}</div><div><div className="project-heading"><div><h3>{project.name}</h3><p className="project-subtitle">{project.subtitle}</p></div>{project.github?<a className="repo-link" href={project.github}>github ↗</a>:project.privateRepo?<span className="repo-private">private repository</span>:null}</div><p className="project-copy">{project.description}</p>{project.metrics?.length?<div className="metrics">{project.metrics.map(x=><span key={x}>{x}</span>)}</div>:null}<div className="tags">{project.tech.map(x=><span key={x}>{x}</span>)}</div></div></article>}
