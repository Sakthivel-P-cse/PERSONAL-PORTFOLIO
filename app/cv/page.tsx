import type {Metadata} from "next";import {Nav} from "@/components/Nav";export const metadata:Metadata={title:"CV",description:"Curriculum vitae of Sakthivel P."};export default function CvPage(){return <><Nav/><main className="shell subpage"><div className="section-label">/cv</div><div className="subpage-head"><div><p className="prompt">sakthivel@portfolio:~$ cat resume.pdf</p><h1>Curriculum Vitae</h1></div></div><div className="pdf-frame"><h2>Resume file hook is ready.</h2><p>Add the supplied CV as <code>public/cv/resume.pdf</code>. GitHub's text-only connector cannot write binary PDF bytes, so the production code intentionally does not point to a missing or fabricated document.</p></div></main></>}
