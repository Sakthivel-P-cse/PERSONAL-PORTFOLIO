import { Nav } from "@/components/Nav";
import { ProjectCard } from "@/components/ProjectCard";
import { TerminalBoot } from "@/components/TerminalBoot";
import { portfolio, projects } from "@/data/portfolio";

export default function HomePage() {
  const skillEntries = Object.entries(portfolio.skills) as Array<[string, readonly string[]]>;
  return <><TerminalBoot /><Nav /><main><section className="hero shell"><div className="hero-kicker">[ system profile / 2026 ]</div><div className="hero-grid"><div><p className="prompt">sakthivel@portfolio:~$ whoami</p><h1>{portfolio.name}</h1><p className="headline">{portfolio.headline}</p><div className="hero-actions"><a className="button primary" href="#projects">view projects</a><a className="button" href="/cv">open cv</a></div></div><aside className="status-panel"><div className="status-line"><span>focus</span><strong>distributed systems</strong></div><div className="status-line"><span>stack</span><strong>go / python / typescript</strong></div><div className="status-line"><span>location</span><strong>kanchipuram, india</strong></div><div className="status-line"><span>status</span><strong className="online">● available to build</strong></div></aside></div></section><section className="shell section"><div className="section-label">01 / about</div><div className="section-grid"><h2>I like systems that stay understandable when things fail.</h2><div className="prose"><p>{portfolio.summary}</p><p>My work spans consensus, storage, telemetry pipelines, security boundaries and agent orchestration. I care about correctness, observability and knowing where a system stops being trustworthy.</p></div></div></section><section className="shell section"><div className="section-label">02 / capabilities</div><div className="skill-grid">{skillEntries.map(([category,items])=><div className="skill-group" key={category}><h3>{category}</h3><p>{items.join(" · ")}</p></div>)}</div></section><section className="shell section" id="projects"><div className="section-label">03 / selected work</div><div className="project-list">{projects.map((project,index)=><ProjectCard project={project} index={index} key={project.name}/>)}</div></section><section className="shell section" id="experience"><div className="section-label">04 / experience</div><div className="timeline">{portfolio.experience.map(item=><article className="timeline-item" key={item.company}><div className="timeline-period">{item.period}</div><div><h3>{item.role}</h3><p className="company">{item.company}</p><p>{item.description}</p></div></article>)}</div></section><section className="shell section two-column"><div><div className="section-label">05 / education</div><h2>{portfolio.education.degree}</h2><p className="muted">{portfolio.education.institution}</p><p>{portfolio.education.period} · {portfolio.education.cgpa}</p></div><div><div className="section-label">06 / signals</div><ul className="signal-list">{portfolio.certifications.map(x=><li key={x}>{x}</li>)}{portfolio.achievements.map(x=><li key={x}>{x}</li>)}</ul></div></section><section className="shell section contact"><div className="section-label">07 / contact</div><p className="prompt">sakthivel@portfolio:~$ ./connect</p><h2>Build something that has to work.</h2><div className="contact-links"><a href={`mailto:${portfolio.email}`}>{portfolio.email} ↗</a><a href={`tel:${portfolio.phone}`}>+91 {portfolio.phone}</a><a href={portfolio.github}>github ↗</a><a href={portfolio.linkedin}>linkedin ↗</a></div></section></main><footer className="shell footer"><span>© {new Date().getFullYear()} Sakthivel P</span><span>built with next.js · shipped without mystery</span></footer></>;
}
